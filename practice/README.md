# practice — 進度紀錄用的圖

把每週想留下來對照的圖**匯出成 JPEG**（Procreate：分享 → JPEG）放在這裡，
建議照課表的命名：`W01D06_圓柱體.jpg`、`W04末_馬克杯.jpg`。

**不要把 `.procreate` 原始檔放進來**（單張常常上百 MB，git 會爆掉）。
原始檔照 [`../00-setup.md`](../00-setup.md) 的規則備份到 iCloud Drive。

這個資料夾最重要的用途是 **W12 的複盤**：那時候你要拿 W1 的圖跟重畫的版本並排比較，
沒有留檔就看不到自己的進步。
